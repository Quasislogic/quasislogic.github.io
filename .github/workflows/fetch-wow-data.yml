name: Fetch WoW Crafting Data

on:
  workflow_dispatch:  # manual trigger
  schedule:
    - cron: '0 6 * * *'  # daily at 6am UTC

jobs:
  fetch-wow-data:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      - name: 🧰 Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: 📦 Create Node.js project & install axios
        run: |
          npm init -y
          npm install axios

      - name: 🧪 Debug: Log Node version and secrets status
        run: |
          echo "Node version: $(node -v)"
          echo "Axios version: $(npm list axios)"
          echo "✅ CLIENT_ID is ${CLIENT_ID:+set}"
          echo "✅ CLIENT_SECRET is ${CLIENT_SECRET:+set}"
        env:
          CLIENT_ID: ${{ secrets.BLIZZARD_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.BLIZZARD_CLIENT_SECRET }}

      - name: 🔄 Fetch WoW data using Blizzard API
        run: node scripts/fetch-wow-data.js
        env:
          CLIENT_ID: ${{ secrets.BLIZZARD_CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.BLIZZARD_CLIENT_SECRET }}

      - name: ✅ Commit and push new data
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add data/
          git commit -m "Update WoW item data"
          git push
